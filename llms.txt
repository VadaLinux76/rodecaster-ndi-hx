# rodecaster-ndi-hx

> Trasforma una webcam USB su Raspberry Pi 4 in una sorgente NDI|HX vera (H.264 via encoder
> hardware bcm2835-codec), scrivendo pacchetti compressi direttamente con l'NDI Advanced SDK
> invece di affidarsi al percorso NDI standard/SpeedHQ. Nato per interoperare con un
> RØDECaster Video, che accetta solo NDI|HX2+ certificato. Licenza MIT; l'NDI Advanced SDK
> necessario per compilare non è incluso (proprietario Vizrt/NDI, si scarica da ndi.video).

Progetto italiano. Il README documenta 6 bug non ovvi scoperti facendo reverse engineering
del wire format compresso dell'NDI Advanced SDK contro un receiver certificato reale (formato
Annex-B vs AVCC, semantica dei FourCC lowest/highest bandwidth, campi obbligatori del frame
compresso, profilo H.264 dell'encoder hardware) — la parte di maggior valore per chi arriva
qui da una ricerca su un errore simile.

## Documentazione

- [README](README.md): overview, requisiti, build, uso, e i 6 bug non ovvi con le relative fix
  (italiano, versione originale/più dettagliata)
- [README.en](README.en.md): stessa cosa in inglese
- [llms-full.txt](llms-full.txt): tutto il contenuto del repository (README + sorgenti +
  script + config) consolidato in un solo file, per chi vuole il contesto completo senza
  seguire i link qui sotto
- [LICENSE](LICENSE): MIT

## Codice

- [annexb.h](annexb.h) / [annexb.cpp](annexb.cpp): parsing Annex-B e ricostruzione delle
  access unit — **nessuna dipendenza dall'SDK NDI**, compilabile e testabile ovunque
- [ndi_hx_send.cpp](ndi_hx_send.cpp): wrapper sottile — usa `annexb` per l'assemblaggio e
  traduce ogni frame in un pacchetto NDI compresso (NDI|HX)
- [ndi_hx_inspect.cpp](ndi_hx_inspect.cpp): tool diagnostico, si collega a una sorgente NDI in
  modalità COMPRESSED e stampa la struttura reale dei pacchetti ricevuti
- [build_hx.sh](build_hx.sh): compila ndi_hx_send (+ annexb.cpp) contro l'NDI Advanced SDK
- [build_inspect.sh](build_inspect.sh): compila ndi_hx_inspect
- [find-ndi-libdir.sh](find-ndi-libdir.sh): helper condiviso (sourceato dai due script sopra)
  che seleziona la sottocartella lib/&lt;arch&gt; giusta per l'host, non semplicemente la prima
- [CMakeLists.txt](CMakeLists.txt): sistema di build alternativo (target build/test/install via
  CTest); costruisce sempre `annexb` + i test, `ndi_hx_send`/`ndi_hx_inspect` solo con
  `-DNDI_SDK_DIR=...`
- [capture_cam_hx.sh](capture_cam_hx.sh): pipeline completa webcam → ffmpeg (encoder hardware
  H.264, fps intero o frazionario tipo 30000/1001) → ndi_hx_send
- [doctor.sh](doctor.sh): autodiagnosi (ffmpeg, encoder hardware, formati webcam, SDK NDI,
  binari, discovery di rete) — solo controlli in lettura, non tocca mai la webcam in streaming
- [ndi-webcam-send.service](ndi-webcam-send.service): unit systemd di esempio per farlo
  partire al boot
- [ndi-webcam-restart.service](ndi-webcam-restart.service) / [ndi-webcam-restart.timer](ndi-webcam-restart.timer):
  timer systemd di esempio per riavviare periodicamente il servizio, workaround per il limite
  di 30 minuti dell'NDI Advanced SDK in modalità "development"

## Test

- [tests/test_annexb.cpp](tests/test_annexb.cpp): test unitari del modulo `annexb` (nessuna
  dipendenza esterna, harness minimale incluso nel file), eseguiti in CI su GCC e Clang con
  warning severi e sotto AddressSanitizer/UndefinedBehaviorSanitizer
